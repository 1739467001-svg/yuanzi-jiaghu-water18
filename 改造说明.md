# 原子江湖 · 改造说明（我具体做了什么）

> 本文用大白话说明：从哪个版本开始、我改了什么、在哪能看到效果、怎么跑起来。
> 所有改动已提交并推送到 `https://github.com/1739467001-svg/yuanzi-jiaghu-water18.git`（`main` 分支，5 个提交，+4270 行 / −255 行，新增 39 个文件）。

---

## 一、起点是什么

改造前，`atom-jianghu` 已经是一个**能跑的 3D 小镇 demo**：

- Three.js 画的小镇（6 座建筑、8 个 Q 版侠客角色、一条河两座桥）
- 56 件真实比赛作品的数据（繁星之夜 38 + 黑客松 18）
- 一个"本地资料演示"的聊天机器人（不接真模型）
- 收藏/记忆存在浏览器 localStorage

**它只是一个"能看"的演示**：没有账号、没有联机、没有运营后台、AI 不会自己看展、没有预算控制。

---

## 二、我具体加了什么（按你最初提的 5 个需求逐条对）

### 1. 侠客 IP 风格、3D 地图与建筑布局

| 改动 | 具体内容 | 在哪看 |
|---|---|---|
| 官方 Logo 接入 | 从你的素材裁出 4 个文件（黑白字标、黑白原子图形标），开机画面和"关于"面板用真 Logo，不再用文字方块 | `public/brand/atomhub-*` |
| 品牌色进入设计系统 | 按 PPT 实测取色：印章红 `#b4432f`、展馆深海蓝 `#16305c`、暖米白 `#f5f4ef` | `src/style.css` |
| **"繁星之夜"夜间主题** | 夜间模式整套重做：深海蓝界面 + 繁星金点缀，小镇和展馆都有星空、月亮、流萤 | 右上角月亮按钮 |
| 展馆星空穹顶 | 展馆夜间变成"室内星河"（服务端权威的星空穹顶 + 展位赛道色灯光） | 夜景进展馆 |
| 首个 GLB 模型接入 | 写了一个"证明 GLB"（二十面体原子核），从 manifest 加载进广场，验证资产管线能跑 | 广场雕塑顶端 |

### 2. AI 日常生活、人物社交、长期记忆

| 改动 | 具体内容 |
|---|---|
| **时辰节律** | AI 按一天 6 个时辰安排去处：清晨茶楼、上午工坊、午后看展、傍晚亭台、入夜展会、深夜早歇。界面天气挂件显示当前时辰 |
| **社交气泡** | 两个 AI 碰面聊天时，头顶浮出话题气泡（"聊起最近看到的作品"） |
| **AI 观展闭环**（PRD H03） | AI 会自己走到展馆、站在展位前看海报、生成不超过 60 字的观感、写入公开记忆，之后在社交里引用（"聊起展会上的《XXX》"）。有完整状态机：计划→移动→观察→生成→完成，作品被撤回立刻取消任务 |
| 观展者可见 | 展馆里能看到 AI 站在展位前，名帖显示"知微 · 观展中"，点击名帖直接打开它在看的作品 |
| **记忆生命周期** | 记忆带 30 天有效期，到期自动停止参与回忆（不静默删除）；关闭记忆时明确二选一："保留已有记录"或"同时删除" |

### 3. 历届比赛展示馆、作品详情与 AI 导览

| 改动 | 具体内容 |
|---|---|
| **结构化优先的 AI 导览** | 新增"呼唤知微导览"按钮。先查结构化事实（有哪些赛道、每个赛道几件作品、某作品详情），引用卡片可点；问"谁是一等奖"会明确说"没有收录"，**不编造** |
| **"带我去"功能** | 导览给的每个推荐都能"带我去"——角色自动走到那个展位前 |
| 展区翻页 | 38 件作品分 5 个展区，展馆内可翻页切换（全房间一致） |
| 新增星火计划赛事 | 用你提供的星火计划手册整理出介绍页（四赛道、时间、奖金），明确标注"作品整理中"，**没有编造任何作品或获奖名单** |
| 赛道色 + 作品亮点 | 从来源展示站恢复 5 个赛道的品牌色，以及作品亮点（如"全程唯一的初中生选手组合"） |

### 4. 素材导入、运营后台、AI Town 改造

| 改动 | 具体内容 |
|---|---|
| **运营后台 `/admin.html`**（从零到有） | 四个页签：目录（草稿/送审/发布/撤回）、导入（差异预览→应用，自动剔除微信号和二维码）、版本（发布快照 + 一键回滚）、审计（每次操作留痕） |
| **后台要登录** | 未登录 401、普通成员 403、运营角色才能操作；提权进审计 |
| **导入引擎重构** | 抽成 `scripts/import-engine.mjs`，可"先预览差异再应用"，重复导入是更新不是覆盖 |
| **资产管线** | `manifest.json` 作为资产唯一登记来源，GLB 优先、失败自动回退程序化模型；附接入文档 |
| **性能基准** | `npm run bench` 自动跑固定路线，测首交互时间、帧率分位、绘制调用数 |

### 5. 首版范围、实施阶段、验收标准

- 全部写进 `docs/`：`DEPLOYMENT.md`（部署）、`WORLD_SERVER.md`（联机协议）、`ASSETS.md`（资产规范）、`PERFORMANCE.md`（性能基线）
- `README.md` 重写，明确"已实现 / 尚未实现"边界，不把演示说成产品

---

## 三、最大的改动：从"单机演示"变成"能联机的产品"

这是之前完全没有的部分，也是代码量最大的地方：

| 能力 | 之前 | 现在 |
|---|---|---|
| 账号 | 无，界面写"访客" | 注册/登录/登出，密码 scrypt 散列，HttpOnly 会话 |
| 联机 | 只能同一浏览器开两个标签页 | **WebSocket 服务端权威**，两台真设备能互见 |
| 移动 | 客户端自己算 | 服务端判定能不能走、算最终落点，非法目标拒绝 |
| 私人聊天 | 无 | 真人对聊：邀请→接受→聊天，支持婉拒/忙碌/超时/屏蔽；**正文不进公开事件流**（有测试保证） |
| 跨区域 | 无 | 小镇↔展馆必须走入口，服务端校验距离，失败保留原地 |
| 断线重连 | 回到出生点 | 记住你离开时的位置和区域，重连恢复 |
| 云端收藏/记忆 | 只存本浏览器 | 登录后同步到账号，本机记录可选迁移 |
| 模型预算 | 无（进程计数，重启归零） | 持久账本：按次去重、预留/结算、可配单价换算金额、超 80% 降级、超 100% 暂停 |
| AI 生活可见性 | 只有本机能看见 | 公开活动流跨设备广播（A 设备的玩家能看到 B 设备房间里 AI 在做什么） |
| 运维观测 | 无 | `/api/world` 看房间/在线人数/公开事件数 |

---

## 四、质量保证（不是我口头说，是跑出来的）

```
npm test           → 59/59 通过（改造前是 18 项）
npm run validate:world → 通过
npm run build      → 通过
npm run test:e2e   → 通过（浏览器自动化，含双设备联机、私聊、降级）
```

测试覆盖了：导航不可作弊（河边只有桥能过）、私聊旁观者收不到正文、撤回作品任务取消、预算阈值、记忆隔离与过期、资产加载兜底等。

**修掉的真实 bug 都有记录**（共 20+ 个），例如：
- 私聊邀请永远发不到对方（路由用错了身份 ID）
- 先加入世界的人看不到后来的人（广播缺字段）
- 断线重连位置恢复失效（游客每次重连换新身份）
- 运营后台的发布覆盖重启就丢 → 改为落盘持久化
- `public/assets` 目录名和构建输出冲突导致静态部署 404

---

## 五、为什么你"看不到改变"

因为**改动都在代码里，需要跑起来才看得见**。三步就能看到：

```bash
cd /Users/mac/Documents/CJY项目/Astra6/atom-jianghu
npm run dev          # 启动（依赖早已装好）
```

打开 http://127.0.0.1:5173 后，这些是**直接可见**的新东西：

1. 点右上角 **月亮按钮** → 整套界面变成"繁星之夜"深蓝+金星，小镇出现星空月亮
2. 点顶部 **武林大会** → 进展馆，等十几秒会看到 **AI 自己走到展位前看海报**（这是全新的）
3. 点展馆里 **"呼唤知微导览"** → 问"有哪些赛道"，它会答结构化事实，点"带我去"角色自动走过去
4. 右下角 **公共展区 1/5** → 翻页换展品
5. 浏览器开**两个窗口**（或两台设备）→ 互相能看到对方化身移动，点击对方名帖能发起私聊
6. 访问 **/admin.html** → 运营后台（需先注册登录）
7. `npm run world` → 单进程部署形态（这就是上云的形态）

---

## 六、文件改动地图（想知道细节看这里）

**新增 39 个文件**，按职责分：

```
server/worldServer.mjs   ← WebSocket 世界服务（房间/权威移动/私聊路由/重连补偿）
server/world.mjs         ← 单进程部署入口（一条命令上云）
server/auth.mjs          ← 账号与会话
server/userStore.mjs     ← 按用户的云端收藏/记忆
server/usageLedger.mjs   ← 模型账本与预算
server/admin.mjs         ← 运营接口（发布/撤回/回滚/审计）
server/opsStore.mjs      ← 运营状态持久化
src/world/net.js         ← 联机客户端（WS 优先、BroadcastChannel 兜底）
src/content/guide.js     ← AI 导览引擎（结构化优先）
src/world/assets.js      ← 资产加载（GLB 优先/程序化兜底）
src/admin.jsx            ← 运营后台界面
admin.html               ← 运营后台入口
docs/*.md                ← 4 份新文档（部署/联机/资产/性能）
tests/*.test.mjs         ← 6 个新测试文件
```

**改动最大的既有文件**：`src/App.jsx`（+约 700 行：账号、导览、迁移、公开事件流）、`src/world/engine.js`（观展状态机+时辰）、`src/world/World.jsx`（星空穹顶、观展者、远程真人渲染）、`src/style.css`（夜间主题+品牌色）。

---

## 七、还没做的（不隐瞒）

- **AI 观展/社交仍是各客户端本地跑**：观感不跨设备同步（服务端只转发公开事件）
- 模型对话要自己填 `ATOM_LLM_API_KEY` 才能接真模型
- 生产部署需要自己配 HTTPS 反向代理、把账号从 JSON 文件迁到数据库

---

*本文档对应提交 `75c1329`，仓库 main 分支。*
